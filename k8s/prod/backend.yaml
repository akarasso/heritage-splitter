apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: heritage
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: heritage-splitter
spec:
  replicas: 1
  strategy:
    type: Recreate          # SQLite = single writer, no rolling update
  selector:
    matchLabels:
      app.kubernetes.io/name: backend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: backend
        app.kubernetes.io/part-of: heritage-splitter
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: ovh-registry
      containers:
        - name: backend
          # I8: Replace :latest with git short SHA at deploy time:
          #   sed -i "s/IMAGE_TAG/$(git rev-parse --short HEAD)/" k8s/prod/backend.yaml
          image: 97ksui70.c1.gra9.container-registry.ovh.net/heritage-splitter/heritage-backend:IMAGE_TAG
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
              name: http
          env:
            - name: DATABASE_URL
              value: "sqlite:///data/heritage.db?mode=rwc"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: heritage-secrets
                  key: jwt-secret
            - name: AVALANCHE_RPC_URL
              value: "https://api.avax-test.network/ext/bc/C/rpc"
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: "3001"
            - name: CHAIN_ID
              value: "43113"
            - name: FACTORY_ADDRESS
              value: "0x3C11E18FaB750B85916501A71a6A621df0CA5ab1"
            - name: MARKET_ADDRESS
              value: "0x3FD5c4216eff75E6988d079A88c43325c3216E99"
            - name: CERTIFIER_ADDRESS
              value: "0xbC03569e83d37Af715Ce0FF1997C6245c8A36d58"
            - name: DOC_REGISTRY_ADDRESS
              value: "0x9f126B09053E450377f1045BCDbbb0d5E6502E2A"
            - name: REGISTRY_ADDRESS
              value: "0x4ed1C6d8DFa89485958287146210d68AfC844A9D"
            - name: DOCUMENT_STORAGE_PATH
              value: "/data/documents"
            - name: CERTIFIER_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: heritage-secrets
                  key: certifier-private-key
            - name: MINIO_ENDPOINT
              value: "http://minio:9000"
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: heritage-secrets
                  key: minio-access-key
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: heritage-secrets
                  key: minio-secret-key
            - name: MINIO_BUCKET
              value: "heritage"
            - name: CORS_ORIGIN
              value: "https://avax.emblem-tech.eu"
          volumeMounts:
            - name: db-storage
              mountPath: /data
          startupProbe:
            httpGet:
              path: /api/health
              port: 3001
            periodSeconds: 2
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /api/ready
              port: 3001
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3001
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
      volumes:
        - name: db-storage
          persistentVolumeClaim:
            claimName: heritage-db
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: heritage
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: heritage-splitter
spec:
  selector:
    app.kubernetes.io/name: backend
  ports:
    - port: 3001
      targetPort: 3001
      name: http
